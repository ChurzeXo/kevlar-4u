node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.deepseek
.vscode

# Kevlar user config (non-sensitive preferences)
skills/kevlar-config.json

# Pro prompt templates (fetched from server, not in public repo)
skills/templates/pro.json

# Rule sets (all served via strategy bundle from kevlar4u.xyz)
skills/rules_*.json

# Ignore all docs except multi-language README translations and ADRs
docs/*
!docs/README.*.md
!docs/decisions/
!docs/DEPLOY.md
!docs/preaudit-pipeline.md
!docs/kevlar-4u-prd.md
!docs/backend-api-requirements.md

# Kevlar runtime state (wizard sessions, drafts)
skills/tmp/

# Persona storage (multi-file JSON — auditors.json + platform files)
# These JSON files are tracked; user-created personas are managed
# through the MCP tools (createPersonaTool / createPersonaWizardTool).
# Ignore draft files under tmp/.
skills/tmp/

# Legacy .md persona files (Phase 1 migration — no longer read by the engine)
# Kept as backup references during the migration period
# skills/**/*.md

# Kevlar lab documents (audit reports, experiments, etc.)
.kevlar_lab/

# Schedule planning (local only, not tracked)
schedule/

# Release guide (local only)
RELEASE.md

# Workbuddy (local editor/tool config)
.workbuddy/

# TypeScript build info
tsconfig.src.tsbuildinfo

# 学习归档目录（本地专用，不提交）
.学习/

# Pro strategy bundle cache (downloaded from kevlar4u.xyz)
skills/strategy-bundle-cache.enc

# Upload bundle for server (one-time use)
.kevlar_upload_bundle.json
.kevlar_backend_task.md
